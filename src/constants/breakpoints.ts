const XS = 0;
const SM = 640;
const MD = 768;
const LG = 1024;
const XL = 1280;
const XXL = 1536;

export const breakpoints = {
    xs: `0px`,
    sm: `${SM}px`,
    md: `${MD}px`,
    lg: `${LG}px`,
    xl: `${XL}px`,
    '2xl': `${XXL}px`,
};

export default { XS, SM, MD, LG, XL, XXL };
